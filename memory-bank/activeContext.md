# Active context

Current work: TASK002 provider and resource monitoring. The latest user policy restores Unknown for genuinely unavailable evidence; the earlier blanket degraded-monitoring policy is superseded.

The registry has 42 components and no heartbeat placeholders. Snapshot v2 supports operational, degraded, outage, maintenance, unknown and not_launched. Real platform data reads, operational queue/receipt counts and media delivery probes extend Supabase database/auth/capacity/product limits, Cloudflare analytics and public provider feeds. Discord and Telegram are monitored separately, so a denied Telegram token cannot obscure verified Discord readiness. Manual payment reconciliation is operational; hosted checkout is explicitly not launched because no provider is configured. Successful data reads prove read availability only. Idle workflows lacking recent success evidence remain Unknown; SMS readiness has an observed device/SIM gap.

All live database read paths succeeded locally, including Supabase product metrics and Cloudflare Worker and zone analytics using existing encrypted Wrangler OAuth. Supabase now publishes 451.9 MiB / 1.93 GiB database disk, 13 / 60 busiest database connections, 333 Auth users, 0 Realtime subscriptions and 1 / 200 pooler clients. Product limits use the same 80% warning threshold as the capacity component. Management billing credentials were not found locally, so organization plan allowances, MAU, egress and Storage billing use remain explicitly unavailable. Live Worker settings report the Standard usage model, so the collector derives its 10,000,000 included monthly request allotment, measures actual UTC-day account-wide Worker usage and confirms the active account inventory: 6 Worker scripts, 53 Pages projects, 1 KV namespace, 1 R2 bucket and 4 Durable Object namespaces; no D1 databases or Queues. Standard Workers, Pages Functions, Paid KV and Paid Durable Objects have no daily hard request/operation cap; R2 allowances are monthly. Billing subscriptions still return HTTP 403, so product billing totals are not inferred. Every expanded row now explains the measured scope, exposes time-aware freshness/response/30-day-success bars, shows product headroom only where a real ceiling exists, and separates observed evidence from possible causes and a next check. The current Partner API Telegram token and all distinct local historical candidates return 401. Discord readiness passes and is independently operational; Telegram alone remains explicitly Unknown. Local OAuth refresh and monitor:watch are integrated and running. All 62 tests and desktop/mobile browser checks pass; TASK002 remains active for external authorization and execution evidence gaps. See docs/actual-telemetry-verification.md. Production Actions, hosting and DNS remain separate activation work.

Incident and maintenance source is now Markdown, not JSON: files under `content/incidents/` require constrained front matter plus a final `## Updates` section. The collector parses and validates them before emitting the public JSON/RSS feed. The public status UI supports the Markdown extension flavor set—CommonMark, GFM tables/task lists/strikethrough/footnotes/autolinks and hard breaks—while raw HTML and executable components remain inert. Local operator templates remain gitignored in `.operator/`. Full suite: 65 tests, data validation and production build passed; a local network-mocked incident verified the public Markdown view without publishing a fabricated record.

The collector converts every incident exposed by the Supabase and Cloudflare public summary feeds into generated records scoped to the matching provider service, then merges them with operator Markdown incidents. A generated record persists and is marked resolved only after a successful later provider feed no longer lists it; failed/rate-limited/malformed feeds preserve it. Provider body payloads are not copied to public output, and a provider incident does not assert application-workflow impact. GitHub Actions runs every 10 minutes and publishes the retained snapshot to the public `status-data` branch, limiting Cloudflare account telemetry collection to at most 144 runs per day; browser refreshes only read the published feed every 10 minutes. Live collection at 2026-09-11T04:27:51Z found five provider-feed records, including all incidents exposed by the current feeds; the public Incident history view rendered all five. Typecheck, the 67-test suite, live collection, data validation and production build passed.
